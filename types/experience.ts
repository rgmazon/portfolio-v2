export type Experience = {
  id: string;
  role: string;
  company: string;
  type: string;
  period: string;
  location: string;
  description: string;
  stack: string[];
};

export type CareerStat = {
  num: string;
  label: string;
};